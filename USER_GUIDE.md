# Nanolayer Studio 使用手册（用户版）

本手册面向日常使用者，重点介绍页面功能与操作流程，不涉及部署与开发配置。

## 1. 页面结构速览

进入页面后，你会看到四个核心区域：

- 左侧：图层面板（导入图片、选择图层、设置参考图层、上下移动、删除）
- 中间：画布工作区（查看结果、移动图层、框选局部编辑区域）
- 右侧：设置面板（AI 调用源、模型、系统指令、模板）
- 底部：提示词输入栏（输入描述并生成/编辑）

## 2. 基本流程

### 场景 A：直接生成图片（文生图）

1. 不需要先导入图片，直接在底部输入提示词。  
2. 点击生成按钮（或按 Enter）。  
3. 结果会作为新图层加入画布。  
4. 可继续在此结果上二次编辑。

适合：从零开始出图、快速尝试多个创意方向。

### 场景 B：编辑现有图片（图生图）

1. 在左侧点击 `+ 添加` 导入图片（也支持粘贴图片）。  
2. 在图层面板点击要编辑的图层，使其成为“当前活动图层”。  
3. 在底部输入修改要求并生成。  
4. 新结果会以新图层形式叠加，原图层不会被覆盖。

适合：换风格、改细节、重绘元素、保留原图做版本对比。

## 3. 局部编辑（只改一块区域）

1. 在左侧工具栏选择“框选/选择”模式。  
2. 在画布上拖动框出要改的区域。  
3. 输入提示词并生成。  
4. 系统会优先按选区执行修改，尽量保持选区外不变。

建议：选区尽量比目标对象稍大，能提升边缘衔接效果。

## 4. 参考图层怎么用

参考图层用于“借鉴风格/角色一致性/材质细节”，不是直接覆盖画面。

操作步骤：

1. 先确保画布里有可参考的图层（导入或历史加载都可以）。  
2. 在左侧图层列表中，点击某图层右侧的“参考图”按钮（小图片图标）。  
3. 被设为参考后，缩略图会有参考标记。  
4. 输入提示词并生成，模型会同时参考这些图层。

补充说明：

- 当前活动图层不能同时作为参考图层。  
- 可选择多个参考图层。  
- 底部输入栏上方会显示当前已选参考图层数量与缩略图。  

## 5. 图层管理

左侧图层面板支持：

- 显示/隐藏图层（眼睛图标）
- 选择活动图层（点击图层卡片）
- 调整上下顺序（上/下箭头）
- 删除图层（垃圾桶）
- 导入新图层（`+ 添加`）

提示：最终导出效果取决于图层顺序与可见状态。

## 6. 模型与 AI 调用源

右侧设置可选择：

- AI 调用源：`fal.ai` 或 `Vertex AI`
- 模型：Nano Banana、Nano Banana Pro、Nano Banana 2、GPT Image 系列（Vertex 下 GPT 不可选）

建议：

- 要速度：优先 Nano Banana  
- 要细节：优先 Pro 或 GPT 系列（在 fal.ai 下）  
- 需要企业侧 Vertex 能力：选择 Vertex AI + Nano Banana 系列

## 7. 系统指令与模板

右侧可填写“系统指令”，用于长期约束模型行为（例如固定风格、角色一致性、构图规则）。

你也可以一键套用模板（如一致性、漫画、角色设定等），再按需求微调提示词。

## 8. 历史记录

顶部 `历史` 可查看过去生成结果，支持：

- 仅复用提示词
- 将历史图加载回当前画布继续编辑

适合做版本迭代与风格复刻。

## 9. 导出

顶部支持：

- 导出 PNG（合并当前可见图层）
- 导出 PSD（保留图层信息，便于继续在设计工具中编辑）

## 10. 提示词写法建议

由于当前页面不再提供“输出比例/质量”下拉参数，如有这类需求，请直接写在提示词中。

可参考模板：

- 风格：`赛博朋克夜景，霓虹灯，高对比，电影感`
- 构图比例：`竖版构图，9:16，主体居中`
- 细节质量：`高细节，清晰纹理，专业插画质感`
- 一致性：`保持角色服装和发型与参考图一致`

## 11. 常见问题

### Q1：为什么结果和我想的不一致？

- 提示词过短或过泛  
- 没有明确主体、风格、构图  
- 参考图层没选对或数量太多导致风格冲突

建议：先缩小目标（只改一件事），再逐步叠加要求。

### Q2：编辑时为什么改动范围太大？

- 建议使用“选区编辑”明确修改区域  
- 提示词里加约束：`仅修改...其余保持不变`

### Q3：如何让同一个角色更稳定？

- 使用同一张角色图作为参考图层  
- 在系统指令中固定角色特征（发型、服装、配色、五官）

---

如果你希望，我可以继续补一版“提示词示例库（按电商图、海报图、头像图、漫画分镜分类）”。
