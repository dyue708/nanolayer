# 前端 Vite 环境变量（复制为 .env 或 .env.local 后按需修改）
# API 基路径（默认使用 Vite 代理 /api）
# VITE_API_BASE_URL=/api
# 飞书网页登录回调地址：须与开放平台「安全设置 → 重定向 URL」、后端 FEISHU_REDIRECT_URI 完全一致（含末尾斜杠）
# VITE_FEISHU_REDIRECT_URI=http://localhost:5173/
